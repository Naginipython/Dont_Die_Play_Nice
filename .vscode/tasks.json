{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake --build build --config Debug --target ALL_BUILD -j 10 --",
            "group": {
                "kind":"build",
                "isDefault": true,
            }
        }
    ]
}